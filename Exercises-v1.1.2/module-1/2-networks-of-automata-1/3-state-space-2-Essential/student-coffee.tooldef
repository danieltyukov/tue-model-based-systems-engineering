from "lib:cif" import *;             //Imports the library necessary to run CIF 3 files

cifsim(
   "student-coffee.cif",
   "-i gui",                          // Allow user input
   "--stateviz=1",                    // Shows the current state in real time
   //"--help",                        // Shows the help text
);
